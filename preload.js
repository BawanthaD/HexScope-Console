// Preload runs in an isolated context before your web code.
// You can expose APIs to window if needed later.
// For now, nothing is required for HexConsole to run.

window.addEventListener('DOMContentLoaded', () => {
  // Just a debug hook if you want
  // console.log('Preload loaded');
});
